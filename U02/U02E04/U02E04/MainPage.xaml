<Page
    x:Class="U02E04.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:U02E04"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Rectangle
            Grid.Row="0"
            Style="{StaticResource Grid1}"/>

        <Rectangle
            Grid.Row="1"
            Style="{StaticResource Grid2}"/>

        <Rectangle
            Grid.Row="2"
            Style="{StaticResource Grid3}"/>

        <Rectangle
            Grid.Row="3"
            Style="{StaticResource Grid4}"/>

        <Rectangle
            Grid.Row="4"
            Style="{StaticResource Grid5}"/>

        <Rectangle
            Grid.Row="5"
            Style="{StaticResource Grid6}"/>


    </Grid>
</Page>
